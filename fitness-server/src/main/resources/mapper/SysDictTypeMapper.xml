<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitness.mapper.SysDictTypeMapper">
    <resultMap id="BaseResultMap" type="com.fitness.entity.SysDictType">
        <id column="id" property="id"/>
        <result column="dict_code" property="dictCode"/>
        <result column="dict_name" property="dictName"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT * FROM sys_dict_type WHERE dict_code = #{dictCode}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_dict_type
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_dict_type (dict_code, dict_name, status)
        VALUES (#{dictCode}, #{dictName}, #{status})
    </insert>
    <update id="updateById">
        UPDATE sys_dict_type SET dict_code = #{dictCode}, dict_name = #{dictName}, status = #{status}
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM sys_dict_type WHERE id = #{id}
    </delete>
</mapper>
